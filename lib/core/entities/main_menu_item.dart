import 'package:flutter/material.dart';

class MainMenuItem {
  MainMenuItem(this.icon, this.title, this.route, this.iconColor);
  final IconData icon;
  final String title;
  final String route;
  final Color iconColor;
}
